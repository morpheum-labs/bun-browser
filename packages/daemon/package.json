{
  "name": "@bun-browser/daemon",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "bin": {
    "bun-browser-daemon": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "bunx --bun tsup",
    "dev": "bunx --bun tsup --watch",
    "lint": "bunx --bun eslint src tsup.config.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@bun-browser/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.7.0"
  }
}
